spring.application.name=hirepath-backend

# Server (Render will inject PORT)
server.port=${PORT:8080}

# Database (from Render environment variables)
spring.datasource.url=${jdbc:mysql://shortline.proxy.rlwy.net:19915/railway?useSSL=false&allowPublicKeyRetrieval=true}
spring.datasource.username=${root}
spring.datasource.password=${syrxyCtJQZEtfIUYwDeRzBnSLFfLfHPs}

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Prevent circular reference issue
spring.main.allow-circular-references=true

# File upload settings
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
